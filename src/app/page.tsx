
// This file can be removed if you prefer to move the (main)/page.tsx content here directly.
// For now, it just ensures that the root path "/" renders the content from (main)/page.tsx.
// Alternatively, configure next.config.js rewrites or just move the content.

import HomePage from "./(main)/page";

export default HomePage;
